[package]
name = "neo-sdk"
version = "0.1.0"
edition = "2024"
description = "Command framework and config for Neo plugins"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = "1"
