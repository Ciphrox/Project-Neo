[package]
name = "plugins"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Links all plugin crates and exposes register_all"

[lib]
path = "lib.rs"

[dependencies]
neo-sdk = { path = "../neo-sdk" }
plugin-help = { path = "help" }
plugin-ping = { path = "ping" }
